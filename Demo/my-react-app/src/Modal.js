export const Modal = () => {
    return (
        <div className="modal">
            <p>Did you complete this task?</p>
            <button onClick={() =>{}}className="btn btn-highlight">Cancel</button>
            <button onClick={() =>{}}className="btn btn-highlight">Confirm</button>
        </div>
    );
}

